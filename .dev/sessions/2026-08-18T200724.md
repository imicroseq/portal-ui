Registered this project in the shared cross-session agent index, corrected a scope-boundary memory error left by a mis-rooted sibling session, and upgraded this project's agentics adoption from 0.14.0 to current (0.15.0 plus unreleased conventions), catching up on several breaking changes.

- `AGENTS.md`: version tag bumped to `0.15.0 (+ unreleased conventions as of 2026-08-18)`, synced `988db215`; added the dispatch-path resolution block (paths are relative to agentics' `template/` directory, not the repo root); renamed `CLAUDE.softeng.md`/`CLAUDE.overture.md` references to `AGENTS.softeng.md`/`AGENTS.overture.md`; added the "another session's work is not yours to pick up" interaction-parameter bullet; added `docs/agent-security.md` to the PR-review and security dispatch rows; added the `upstream-check.md`, `context-economy.md`, and role (`AGENTS.roles/<role>.md`) dispatch rows; Initialization section updated to the current role-based model, with all questions already answered from existing project memory annotated inline
- `~/.claude/agent-index.md`: registered this session as "iMicroSeq Portal UI" (project `iMicroSeq/virusseq-portal`); synced the shared file's own Members-lookup rule text to the current (partially uncommitted) agentics convention, since a stale copy still said to break ties by recency, a rule since withdrawn after three same-day misroutes
- Project memory (`feedback_scope_boundary.md`, `project_agentics_config.md`): corrected a scope-boundary fact that a submission-service sibling session, mis-rooted in this repo by the multi-root workspace, had written into this project's memory space backwards (claiming this session's job was submission-service, portal-ui belonged elsewhere). Confirmed directly by the developer: this session's specialty is portal-ui itself. Trimmed a since-redundant incident narrative once the general lesson was abstracted upstream into agentics' own conventions (a mis-rooted session writing into the wrong project's memory; a legitimate correction misread as tampering)
