Synced this project's agentics adoption from 0.15.0 to 0.18.0 plus unreleased conventions as of today, and re-migrated this session's agent-index registration twice in the same sitting as the ownership-registry schema itself changed mid-session.

- `AGENTS.md`: version tag bumped to `0.18.0 (+ unreleased conventions as of 2026-08-20)`, synced `27a03db8` (the last released and pushed commit, not current HEAD, per the new "never stamp an unpushed SHA" rule); added the "acknowledging a correction is not making it" interaction-parameter bullet; added `conventions/entry-formats.md` and `conventions/agent-troubleshooting.md` dispatch rows; replaced the one-line memory-hygiene note with the much-expanded current version (where project memory actually lives, why it's the workspace's and never a place for a carry-forward instruction, the one legitimate exception, and the `.claude/memory/`-inside-the-repo anti-pattern)
- `~/.claude/agent-index.md`: migrated this session's own Member entry from the retired `id`/`focus`/`project`/`scope` schema to the current ownership registry (`owns: iMicroSeq`, `expert`, `assigned`), confirmed with the developer first since claiming the broad `iMicroSeq` prefix is a structural authority claim, not just a reformat; migrated a second time in the same session when the field itself renamed (`window` to `workspace`) and "family head" was retired as a term; left the shared header and every other session's entry untouched after being corrected for proposing to fix the header myself, that upkeep belongs to the agentics session
- Project memory (`feedback_scope_boundary.md`, `project_agentics_config.md`): dropped "family"/"family head" language to match the terminology retirement, recorded the sync history and the header-editing correction

Also brought `virusseq-infra-sd4h`'s `.dev/` up to date at the developer's request: migrated its single-file `.dev/sessions.md` to the per-day `.dev/sessions/` format (one backfilled entry, `2026-06-21T000000.md`) and deleted the old file. Scoped narrowly per the developer's own choice: no `AGENTS.md`/`CLAUDE.md` bootstrap there, since that repo has never formally adopted agentics and this was explicitly just a `.dev/` cleanup, not a full adoption.
