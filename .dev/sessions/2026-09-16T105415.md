Synced this project's agentics adoption from 0.18.0 to 0.20.0 plus unreleased conventions, per direct developer instruction, reconciling against the current template's actual content rather than walking through the 171 changelog entries individually given the scale.

- `AGENTS.md`: version tag bumped to 0.20.0 (+ unreleased as of 2026-09-16, synced `2d38a6d`); added four new interaction-parameter bullets (name silently-resolved ambiguity, a topic reference isn't a go-ahead, agreement needs to carry evidence it was checked, a question isn't a work order); added two dispatch rows (`conventions/initialization.md`, `conventions/git.md`, the latter now split out of `session-discipline.md`); removed the inline `## Initialization` section entirely, matching the current template, since its answers already lived durably in project memory
- `project_agentics_config.md`: sync history updated; noted the Initialization section's removal so project memory is now the only recorded copy of those answers
